{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:app": "npm start --prefix ../",
    "start:app:wait": "npm start --prefix ../ && npx wait-on http://localhost:4100",
    "test": "npx cypress open",
    "test:ci": "npx cypress run",
    "run:test": "npm run start:app && npm run test",
    "run:test:ci": "npm run start:app && npm run test:ci"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^4.0.2",
    "ts-loader": "^6.2.2",
    "typescript": "^3.8.3",
    "webpack": "^4.42.1"
  },
  "dependencies": {
    "indexed-db-utilities": "^2.1.1",
    "wait-on": "^4.0.1"
  }
}
