// const { build } = require('./program');
// const data = require('../resources/channelschedule.json');

test('programs correctly built', async done => {
  // const programs = build(data.schedule);
  // console.log('programs', programs.length);
  // console.log(programs[0]);
  // console.log(programs[100]);
  // console.log(build);
  expect(1 + 2).toBe(3);
  done();
});
