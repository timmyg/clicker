schema {
  query: Query
  mutation: Mutation
}

type Query {
  getBox(id: ID): Box
}

type Mutation {
  addBox(id: ID!, locationId: String!, zone: String!): Box!
}

type Box {
  id: ID!
  locationId: String
  zone: String
}
